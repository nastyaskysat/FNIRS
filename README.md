# FNIRS Анализатор

Система для анализа данных функциональной ближней инфракрасной спектроскопии (fNIRS) с графическим интерфейсом и поддержкой работы в реальном времени.

## 📋 Описание

Система предназначена для обработки данных с фотометра Arduino, расчета концентраций гемоглобина и визуализации результатов в реальном времени.

##  Структура проекта

```
FNIRS/
├── backend/                 
│   ├── core/               
│   ├── serial/             
│   └── analysis/          
├── frontend/               
│   ├── gui/               
│   └── widgets/           
├── data/                  
├── main.py               
├── config.py              
├── requirements.txt      
├── Dockerfile
├── docker-run.sh         # Скрипт запуска для Linux/macOS
├── docker-run.bat        # Скрипт запуска для Windows
├── universal-run.sh      # Универсальный скрипт для Linux/macOS
├── universal-run.bat     # Универсальный скрипт для Windows
└── README.md
```

##  Быстрый запуск

### Универсальные скрипты (рекомендуется)

**Для Linux/macOS:**
```bash
./universal-run.sh
```

**Только через Docker:**
```bash
./docker-run.sh
```

**Только локальная версия:**
```bash
pip install -r requirements.txt
python3 main.py --gui
```

### Windows

**Универсальный скрипт (автоматически выбирает Docker или локальный запуск):**
```cmd
universal-run.bat
```

**Только через Docker:**
```cmd
docker-run.bat
```

**Только локальная версия:**
```cmd
pip install -r requirements.txt
python main.py --gui
```

## Использование

1. Подключите Arduino с скетчем `photometer.ino`
2. Запустите приложение
3. Выберите порт Arduino
4. Начните сбор данных
5. Анализируйте файлы

